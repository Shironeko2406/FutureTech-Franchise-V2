import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const WorkTemplateReducer = createSlice({
  name: "WorkTemplateReducer",
  initialState,
  reducers: {}
});

export const {} = WorkTemplateReducer.actions

export default WorkTemplateReducer.reducer